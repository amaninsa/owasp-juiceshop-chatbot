# Example only — DO NOT commit real API keys.
# Create the Secret out-of-band:
#   kubectl -n juiceshop-chatbot create secret generic juiceshop-chatbot-secrets \
#     --from-literal=OPEN_AI_KEY="sk-..." \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# This file is intentionally NOT included in any kustomization.
apiVersion: v1
kind: Secret
metadata:
  name: juiceshop-chatbot-secrets
  namespace: juiceshop-chatbot
  labels:
    app.kubernetes.io/name: juiceshop-chatbot
    app.kubernetes.io/part-of: juiceshop-chatbot
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  OPEN_AI_KEY: ""
