# Child Applications managed by the App-of-Apps root.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - application-local.yaml
  - application-dev.yaml
  - application-prod.yaml
  - application-monitoring.yaml
