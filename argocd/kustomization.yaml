# Bootstrap ArgoCD: AppProject + App-of-Apps root.
# Child Applications live under argocd/apps/ and are synced by the root app.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - project.yaml
  - root-application.yaml
