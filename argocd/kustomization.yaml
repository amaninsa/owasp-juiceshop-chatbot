# Bootstrap ArgoCD Applications + Project together
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - project.yaml
  - application-local.yaml
  - application-dev.yaml
  - application-prod.yaml
