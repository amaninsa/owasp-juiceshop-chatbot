# Example OpenAI credentials for local / compose (do NOT commit real keys).
# Copy to .env.openai at the repo root.

OPEN_AI_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
