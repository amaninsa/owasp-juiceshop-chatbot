# CodeQL is owned by security.yml (matrix: javascript-typescript + python).
# This file remains for workflow_dispatch / badge compatibility only.
name: "CodeQL Scan"

on:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use Security workflow
        run: echo "Run the Security workflow (.github/workflows/security.yml) for CodeQL."
