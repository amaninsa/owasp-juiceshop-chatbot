name: AI Platform CI/CD (legacy)

# Superseded by ci.yml / cd.yml / security.yml / release.yml.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Why run the legacy pipeline?"
        required: false
        default: "compat"

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Use enterprise workflows
        run: |
          echo "::notice::Use ci.yml, cd.yml, security.yml, and release.yml."
